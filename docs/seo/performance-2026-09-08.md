# Мобильная производительность и доступность

## Условия измерения

Lighthouse 13.4.1, мобильная эмуляция по умолчанию, headless Chrome, публичная главная `https://new.odintsovclinic.ru/`. Исходный запуск: 08.09.2026, 22:09 UTC, до публикации SEO-изменений. Это лабораторный снимок с симуляцией условий; он не является полевыми Core Web Vitals реальных пользователей.

| Показатель | До публикации | После публикации |
|---|---:|---:|
| Performance | 81/100 | 82/100 |
| Accessibility | 95/100 | 95/100 |
| Best Practices | 100/100 | 100/100 |
| SEO | 69/100 | 69/100 |
| First Contentful Paint | 1,7 с | 1,6 с |
| Largest Contentful Paint | 3,4 с | 3,3 с |
| Total Blocking Time | 0 мс | 0 мс |
| Cumulative Layout Shift | 0 | 0 |
| Speed Index | 14,6 с | 14,5 с |

SEO-оценка снижена, в частности, намеренным запретом индексации staging. Ради этой оценки нельзя отключать `noindex`. Score Lighthouse не является оценкой позиции в поиске. Speed Index на странице с самостоятельно меняющейся каруселью требует осторожного чтения; не следует объявлять его скоростью готовности записи к работе.

## Конкретные наблюдения

| Приоритет | Наблюдение Lighthouse | Следующее действие и проверка |
|---|---|---|
| P2 | Изображения загружены крупнее отображаемого размера; расчётный резерв около 149 КиБ, в том числе портреты 600×800 при показе около 429×571 | Подготовить несколько размеров WebP/AVIF, задать подходящий `srcset/sizes`; сохранить прозрачные края, резкость и текущую геометрию карусели |
| P2 | Возможная задержка первого рендера из-за ресурсов — около 280 мс в этом запуске | Проверить критический CSS и последовательность загрузки; не удалять стили на основании одной оценки |
| P2 | Акцент `#1c89a1` на белом и белый текст на этом акценте имеют контраст около 4,08:1 | Отдельно подобрать оттенок для небольшого текста и кнопок, проверить состояния hover/focus и визуальный бренд |
| P2 | Вторичный текст `#6b7280` на поверхности `#eff4f5` даёт около 4,35:1 | Проверить все сочетания небольшого текста и фона; увеличения размера самого по себе недостаточно для каждой подписи |
| P2 | Блок «ВАБ как щадящая технология» нарушает последовательность заголовков | Проверить полный контур H2/H3 страницы и изменить уровень без изменения оформления |
| P2 | Некоторые ссылки рейтингов и услуг имеют доступное имя, не совпадающее с видимой подписью | Проверить доступные имена в DOM и сценарий управления голосом; сохранить понятный контекст назначения |

Эти замечания зафиксированы отдельно от SEO-разметки. Текущая поставка исправляет геометрию обложек блога и ошибочные OG-размеры, но не объявляет выполненными общую переработку изображений, цветовую систему или ускорение карусели.

## Проверка после публикации

Повторный запуск выполнен 08.09.2026 в 22:47 UTC после публикации `1ed976d`, той же версией Lighthouse и в том же мобильном режиме. Детали находятся в [протоколе поставки](verification-2026-09-08.md). Разницу в один балл Performance нельзя уверенно приписать изменениям сайта: два одиночных запуска подвержены шуму сети и машины. Для решения о производительности после запуска на основном домене нужны полевые данные Search Console/CrUX и реальные события Метрики, если они доступны.

Команда воспроизведения: `npx --yes --package lighthouse lighthouse https://new.odintsovclinic.ru/ --chrome-flags='--headless --no-sandbox' --output=json --output-path=/tmp/clod-lighthouse.json --only-categories=performance,seo,accessibility,best-practices --quiet`. Lighthouse запускается как отдельный инструмент, не добавляется в зависимости приложения.

**Что сделано:** сохранён исходный мобильный замер и конкретные причины оставшихся замечаний.

**Что нужно от вас:** для текущего SEO-деплоя ничего; дальнейшие изменения цветов и карусели оценивать как отдельную UI-задачу.

**Как увидеть результат:** открыть этот протокол и итоговую проверку; исходный JSON локального измерения — `/tmp/clod-lighthouse-before.json`.
