---
description: Error handling and validation patterns for React components and JavaScript modules
globs:
  - src/**/*.jsx
  - src/**/*.js
alwaysApply: false
---

# Error Handling & Validation

## Guard Clauses First
Handle errors and edge cases at the top of functions so the happy path stays obvious.

```jsx
// ✅ GOOD
function DoctorCard({ doctor }) {
  if (!doctor) return null
  if (!doctor.name) return <SkeletonCard />
  return <div>{doctor.name}</div>
}

// ❌ BAD
function DoctorCard({ doctor }) {
  if (doctor) {
    if (doctor.name) {
      return <div>{doctor.name}</div>
    }
  }
}
```

## Early Returns Over Else
Avoid `else` after a `return` when a guard clause makes the branch simpler.

```jsx
// ✅ GOOD
if (isLoading) return <Spinner />
if (hasError) return <ErrorMessage message={error} />
return <Content />

// ❌ BAD
if (isLoading) {
  return <Spinner />
} else if (hasError) {
  return <ErrorMessage />
} else {
  return <Content />
}
```

## Fail Fast

- Throw errors early with rich context for invalid state, misconfiguration, or broken invariants
- Never silently swallow exceptions — either handle them or propagate them
- In API handlers, fail closed: reject first, then proceed with valid data
- For missing production credentials (SMTP, TOKEN_SECRET), fail immediately with a clear error instead of degrading silently

## Validate at the Boundary

- In API handlers, validate request data before side effects
- In React forms, validate before starting async work
- Keep normalization close to the boundary: `trim`, whitelist, parse, and reject early
- Move reusable validation rules into `src/lib/**` instead of duplicating them across components and endpoints

## Form Validation
- Validate on the client before any async call
- Show inline errors next to fields, not only as toasts
- Disable the submit button while `isSubmitting` is true
- Preserve user input on validation failure when possible

## Error State Naming
```jsx
const [isLoading, setIsLoading] = useState(false)
const [hasError, setHasError] = useState(false)
const [errorMessage, setErrorMessage] = useState('')
```

## Async Calls
```jsx
// ✅ GOOD - explicit error boundary
async function submitAppointment(data) {
  setIsLoading(true)
  try {
    await api.post('/appointments', data)
    onSuccess()
  } catch (err) {
    setErrorMessage('Не удалось отправить заявку. Попробуйте ещё раз.')
    setHasError(true)
  } finally {
    setIsLoading(false)
  }
}
```

## Server Errors

- Return explicit HTTP status codes and machine-readable JSON
- Use a stable JSON shape for API failures: `{ error: string }`
- Error messages: single sentence, no trailing period, include full context
- Log enough context to diagnose the issue without leaking secrets or excessive PII
- Do not swallow exceptions silently
- Keep try/catch scope as small as possible — don't wrap entire functions

## Error Messages

```js
// ✅ GOOD - specific, contextual, no trailing period
throw new Error(`Doctor not found for slug: ${slug}`)

// ❌ BAD - vague, no context
throw new Error('Something went wrong.')
```
