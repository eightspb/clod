# Поэтапное исправление замечаний SEO/GEO

Продолжение аудитов от 8 сентября 2026 года. Начальная версия — `5cfcb0d`; технические исправления первого аудита уже опубликованы в `1ed976d`.

**Опубликовано:** `379c41b`, 08.09.2026 в 23:30 UTC, [new.odintsovclinic.ru](https://new.odintsovclinic.ru/). Все четыре технических этапа ниже завершены. [Полный CI](https://github.com/eightspb/clod/actions/runs/34290428473) успешен: 2 405 unit и 154 E2E.

## Этапы и границы

| Этап | Исправления | Приёмка |
|---|---|---|
| 1. Достоверные данные и ссылки | Единые внешние карточки в видимом контенте и JSON-LD; 150+ обученных врачей; реальные значения счётчиков в серверном HTML | Компонентные тесты и HTML без JavaScript |
| 2. Мобильная загрузка и доступность | Responsive-портреты, контраст небольшого текста и кнопок, последовательность заголовков, имена ссылок | Unit, браузер на полной странице, Lighthouse |
| 3. Подготовка миграции | Релевантная цель общего УЗИ, устранение переходов несвязанных услуг на главную, проверка индексации основного домена | Unit, HTTP/E2E, live-аудит |
| 4. Поставка | Общие тесты, lint, build, CI, штатный деплой с бэкапом | Smoke, live-обход и целевые браузерные проверки |

Этапы 1–3 выполнены параллельно по отдельным файлам тремя агентами и общим исполнителем, с независимым ревью. Общий релиз также включает отдельные пользовательские задачи о датах блога, прокрутке боковой колонки и ссылке на гинекологов. DNS, индексация staging и внешние карточки не менялись. Медицинское рецензирование не приписывалось.

## Подтверждение владельца

8 сентября владелец подтвердил использование опубликованных цифр и попросил заменить число обученных врачей на «больше 150». Распределение процедур по годам сохранено до получения более точных данных. География, показатели процедур и рейтинги остаются по указанию владельца. На публичном сайте не добавляются подписи о неподтверждённых данных. Это подтверждение владельца, а не независимая проверка исходного реестра.

## Оставшиеся внешние этапы

- Доступы к Яндекс Вебмастеру, Search Console и Метрике для фактических показов, индексации и конверсий; доступ владельца к карточкам организации.
- Подтверждение состава старых услуг и статуса отсутствующих врачей для окончательной карты переноса. Отсутствие релевантного нового URL само по себе не означает закрытие услуги.
- Медицинское рецензирование страниц и статей уполномоченным врачом; точные данные распределения ВАБ по годам от владельца.
- Отдельно согласованное окно переноса основного домена. Повторная GEO-панель после переноса и обхода поисковыми системами.

## Проверки

Перед правкой тесты воспроизвели три ошибки страницы результатов (разные внешние сущности и нулевые счётчики) и семь ошибок миграции. После исправления они проходят. Точные протоколы независимых участков: [портреты](portrait-fix-2026-09-08.md), [доступность](accessibility-fix-2026-09-08.md), [миграция](migration-fix-2026-09-08.md).

- Итоговый CI: 143 файла, 2 405 unit; 154 E2E, включая Pagefind; ESLint, production-сборка, аудит зависимостей и Docker image — success.
- Первичный локальный браузерный прогон: 105 сценариев прошли, один существующий сценарий Pagefind пропущен локально; финальный CI выполнил его успешно.
- Аудит сборки + SSR: 76 URL, ноль ошибок и предупреждений. `indexabilityChecked: false` для локальной проверки.
- Финальный live-аудит: 76 URL, ноль ошибок и предупреждений. Все 76 страниц сохраняют HTTP noindex; `indexabilityChecked: false` ожидаем для staging.
- Финальный live-браузер: 34 сценария, включая главную, блог, миграцию и сетевой тест портретов, прошли. Использованы тесты из опубликованного коммита, без локального dev-сервера.
- Штатный `bun run deploy`: резервная копия, публикация `sha-379c41b`, smoke `/api/health` → 200, `/` → 200, `/doctors` → 200, `/api/admin/stats` → 401.
- Мобильный viewport 390×844, полная главная при `scrollY = 0`: прозрачные края, геометрия слоёв и две строки имени сохранены; браузер реально выбирает уменьшенные WebP.
- Lighthouse 13.4.1: исходная и финальная live-главная — Accessibility 95 → 100/100, неуспешных accessibility-аудитов нет. Это автоматическая проверка в конкретном режиме, не полная сертификация доступности.
- Независимое ревью подтвердило корректность `srcset/sizes` и обнаружило два граничных случая robots.txt; исправлены специфичность правил с `$`/`*` и варианты `Googlebot*`/`Googlebot/1.2`. Итоговый набор аудитора — 34 теста, включая Red → Green для замечаний ревью.

Первая публикация `199d856` прошла backup и smoke. Live-аудит проверил 76 страниц без ошибок и предупреждений; все 76 сохранили HTTP noindex. На живом сайте прошли 28 браузерных сценариев главной, блога и миграции. В серверном HTML `/nashi-rezultaty` подтверждены `150+` и правильные внешние карточки.

Полный CI первой версии: 2 399 unit прошли, 144 E2E прошли и два выявили смену фона отключённых кнопок записи при hover. Исправлена одна CSS-декларация, добавлены три цветовых регрессионных теста; локально прошли все 11 сценариев `booking-layout` и 45 целевых unit. Поправка включена в финальный успешный CI и публикацию.

Замер опубликованного `199d856` обнаружил второй размерный запрос при автопрокрутке: 8 из 9 врачей загружали сначала 360 px, затем 480 px. `sizes` карусели теперь соответствует активному портрету на всех слоях; это сохраняет один размер ресурса при полном цикле. Сетевой E2E на DPR 1,75 прошёл Red (8 дублей) → Green (9 врачей, 0 дублей), вместе с 20 остальными сценариями карусели. CSS глубины и размеры на экране не изменены.

## Финальное измерение загрузки

Lighthouse 13.4.1, публичная главная, стандартная мобильная эмуляция, headless Chrome. Исходный запуск — 23:01 UTC (`1ed976d`), итоговый — 23:31 UTC (`379c41b`).

| Показатель | До | После |
|---|---:|---:|
| Передано изображений, включая логотип и HTTP-накладные расходы | 311 945 байт | 218 886 байт (−29,8%) |
| Performance | 85 | 82 |
| Accessibility | 95 | 100 |
| Best Practices | 100 | 100 |
| SEO | 69 | 69 |
| FCP | 1,27 с | 1,68 с |
| LCP | 2,94 с | 3,20 с |
| TBT | 0 мс | 0 мс |
| CLS | 0,02461 | 0,00010 |
| Speed Index | 12,53 с | 14,20 с |

Байтовая экономия подтверждается сетевым журналом: каждый из девяти врачей загружает один 480 px ресурс. Два повторных обращения к тому же URL обслужены кешем с нулевой передачей байт; второй размер не скачивается. Промежуточная версия давала 351 551 байт и устранена именно по этому измерению.

Ускорение страницы целиком не доказано: в промежуточном запуске LCP составлял 2,65 с, в финальном — 3,20 с. Одиночные лабораторные запуски зависят от сети и нагрузки; Speed Index также зависит от автопрокрутки. Запрет индексации staging сохранён, поэтому SEO-score не использовался как основание отключать noindex. Следующий этап производительности — повторяемые замеры критического CSS и полевые данные после переноса.

Сырые артефакты этого сеанса: `/tmp/clod-lighthouse-followup-before.json`, `/tmp/clod-lighthouse-followup-after.json`, `/tmp/clod-lighthouse-followup-final.json`, `/tmp/clod-seo-followup-final-live.json`. Они не включены в git; этот документ сохраняет результаты и условия измерения.

**Что сделано:** опубликованы исправления данных/ссылок, портретов, доступности и миграции; пройдены общий CI и live-проверки.

**Что нужно от вас:** доступы и подтверждения для внешних этапов из списка выше.

**Как увидеть результат:** [новый сайт](https://new.odintsovclinic.ru/), [результаты с 150+ обученных врачей](https://new.odintsovclinic.ru/nashi-rezultaty), [исправленный переход УЗИ](https://new.odintsovclinic.ru/usd), протоколы участков выше.
