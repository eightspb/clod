# Проверка SEO-поставки

Дата: 8 сентября 2026 года. Исходная версия: `11cc2a8`. Рабочая ветка: `codex/seo-geo-audit-2026-09-08`.

## Локальная проверка

| Проверка | Результат |
|---|---|
| Vitest, вся база | 139 файлов, 2 332 теста — успешно |
| ESLint | Успешно |
| Production build Astro + Pagefind | Успешно; 75 статических страниц в поисковом индексе |
| Обход работающей production-сборки, включая SSR `/gynecology` | 76 URL; 0 ошибок, 0 предупреждений; sitemap и внутренние SEO-изображения загружались по HTTP |
| Playwright: блог, навигация, главная, поиск Pagefind, миграционные редиректы | 27 сценариев — успешно, без пропусков |
| Визуальная проверка | Статья и маммология при ширине 390 px; статья также на desktop, содержание и автор, календарная дата 1 апреля / обновление 8 сентября |
| Отдельное ревью | Исправлены связь ProfilePage с Person, проверка реального sitemap/assets в live-аудите, тип процедуры, даты и медицинская vocabulary |
| Git diff whitespace | Без ошибок |

Поведенческие изменения проходили Red → Green: ссылки и сущности, контракты live-аудита, дата без сдвига по часовому поясу, медицинская разметка, семь старых URL. Для миграции все семь сценариев сначала падали на отсутствующем или неверном ответе и прошли после исправления и новой сборки.

Браузерные тесты запускались на отдельном порту 4359 с собранным приложением и временной локальной базой. Первоначальный прогон на 4322 попадал в оставшийся dev-сервер с устаревшим кешем Vite; после выделения production-порта ошибка не воспроизвелась. Итоговый прогон включал проверку Pagefind с `CI=1`, поэтому её условный локальный skip не скрывал результат.

Команды: `bun run test:run`, `bun run lint`, `bun run build`; `bun run audit:seo --base-url http://127.0.0.1:4359 --live --output /tmp/clod-seo-release-local.json`. Playwright использовал существующие настройки проекта с временным конфигом, отключающим повторный запуск сервера, и явным адресом этой production-сборки.

Локальные логи: `/tmp/clod-seo-release-tests.log`, `/tmp/clod-seo-release-lint.log`, `/tmp/clod-seo-release-build.log`, `/tmp/clod-seo-release-e2e.log`, `/tmp/clod-seo-release-local.json`. Они не входят в git и могут быть удалены системой как временные файлы.

## Публикация

Опубликована версия `1ed976d1041b70032df52d9c85b294946879cd89`, образ `ghcr.io/eightspb/clod:sha-1ed976d`. [GitHub Actions](https://github.com/eightspb/clod/actions/runs/34287052813) завершился успешно: Lint, Unit tests, Security audit, Build, E2E, Docker image. В CI прошли 2 332 модульных теста и все 139 браузерных сценариев.

`bun run deploy` завершился успешно 08.09.2026 около 22:46 UTC. Перед обновлением создана локальная резервная копия `/srv/backups/clod/daily/clod-20260908T224013Z.tar.gz`; конфигурация nginx прошла проверку. Предыдущий образ `sha-11cc2a8` сохранён для штатного `bun run rollback`.

| Проверка публичного сайта | Результат |
|---|---|
| Штатный smoke | `/api/health`, `/`, `/doctors` — 200; `/api/admin/stats` без авторизации — 401 |
| Полный live SEO-обход в 22:47 UTC | Все 76 URL sitemap — 200, ошибок 0, предупреждений 0; sitemap и внутренние SEO-изображения проверены на новом хосте |
| HTTP-запрет индексации | Все 76 страниц сохраняют `X-Robots-Tag: noindex, nofollow`; запрет также подтверждён для sitemap, robots и 404 |
| Несуществующий URL | 404, без перенаправления на главную |
| Блог и миграция в браузере на публичном хосте | 11 сценариев успешно: четыре проверки статьи и семь постоянных переходов с проверкой целей |
| Основной домен | `odintsovclinic.ru` — 200, заголовок и ресурсы Tilda; переноса DNS не было |
| Мобильная статья после публикации | Проверена при 390×844: шапка, заголовок, автор, дата публикации и дата обновления; видимого смещения даты нет |

Lighthouse после публикации, тот же мобильный режим и главная: Performance 82, Accessibility 95, Best Practices 100, SEO 69; FCP 1,6 с, LCP 3,3 с, TBT 0 мс, CLS 0, Speed Index 14,5 с. Изменение Performance 81 → 82 находится в пределах возможного шума одиночных лабораторных запусков и не объявляется доказанным ускорением. SEO 69 по-прежнему включает намеренное закрытие staging от индексации. Подробности — в [протоколе производительности](performance-2026-09-08.md).

Итоговые локальные артефакты: `/tmp/clod-seo-live.json`, `/tmp/clod-seo-live-summary.log`, `/tmp/clod-seo-http-after.jsonl`, `/tmp/clod-seo-live-e2e.log`, `/tmp/clod-lighthouse-after.json`, `/tmp/clod-seo-deploy.log`. Сам ключ Яндекса в них не записывался. Этот протокол дополнен отдельным коммитом документации после проверки; исходный код приложения соответствует проверенному и опубликованному `1ed976d`.

**Что сделано:** подтверждены локальная сборка, полный CI, публикация, SEO-контракты и переходы на работающем сайте.

**Что нужно от вас:** ничего для проверки и публикации нового сайта.

**Как увидеть результат:** открыть [новый сайт](https://new.odintsovclinic.ru/) и [общий аудит](audit-2026-09-08.md).
